# Real backend URL (used only when VITE_USE_MOCK_API=false)
VITE_API_BASE_URL=http://localhost:5000/api
VITE_APP_NAME=One Enterprise
VITE_ENVIRONMENT=development
# When true, auth APIs hit /api/* on the Vite dev server
VITE_USE_MOCK_API=true
